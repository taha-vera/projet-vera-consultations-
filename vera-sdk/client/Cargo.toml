[package]
name = "vera-client-sdk"
version = "0.1.0"
edition = "2021"
description = "VERA SDK for AI operators — buy legal cultural data"

[dependencies]
vera-spine = { path = "../../vera-sib/spine" }

[lib]
name = "vera_client_sdk"
path = "src/lib.rs"
