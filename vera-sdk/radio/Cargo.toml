[package]
name = "vera-radio-sdk"
version = "0.1.0"
edition = "2021"
description = "VERA SDK for radio operators — 3-line integration"

[dependencies]
vera-spine = { path = "../../vera-sib/spine" }

[lib]
name = "vera_radio_sdk"
path = "src/lib.rs"
