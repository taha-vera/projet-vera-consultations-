[package]
name = "vera_attribution"
version = "0.1.0"
edition = "2021"

[dependencies]
zeroize = { version = "1.7", features = ["derive"] }
rand = "0.8"
uuid = { version = "1.8", features = ["v4"] }
base64 = "0.22"

[lib]
name = "vera_attribution"
path = "src/lib.rs"