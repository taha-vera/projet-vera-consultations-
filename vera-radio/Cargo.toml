[package]
name = "vera-radio"
version = "0.1.0"
edition = "2021"

[dependencies]
vera-spine = { path = "../vera-sib/spine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2", features = ["native-tls"] }

[dev-dependencies]
proptest = "1"
